/* exported getLastChar */

// find the length of the string using length - 1
// use that number for the last character of the string
// output the last character

function getLastChar(string) {
  return string[string.length - 1];
}
